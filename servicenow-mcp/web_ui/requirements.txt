# Web UI specific requirements
flask>=2.3.0
flask-cors>=4.0.0

# Production server
gunicorn>=21.0.0

# AWS SDK (replaces AWS CLI dependency)
boto3>=1.34.0

# These are also in the main requirements.txt
requests>=2.28.0
python-dotenv>=1.0.0

# Database-backed Authentication
bcrypt>=4.0.0  # Password hashing (recommended)
# argon2-cffi>=21.0.0  # Alternative: Argon2 hashing (optional)

# Note: SQLite is included in Python standard library
